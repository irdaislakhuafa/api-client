package com.sample.apiclient.helpers;

import lombok.Getter;

public class MessageHelper {
    @Getter
    private static String serverNotRunning = "Connection with API failed, maybe API endpoint not running!";
}
